age <- c(21, 25, 31, 45, 52)
jimi<- mean(age)
jimi

# age is an object,
#() is list
#c () functions that combines values to vector
#<-assignment operator
#jimi is another object
#is comment

4+5

a<-5
b<-4
mean(c(a,b))
# rm(list = ls()) reset envionment
